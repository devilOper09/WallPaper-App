import React from 'react'

const imageUrl = wallpaper.image_url.startsWith("http")

function download() {
    const downloadFileAtURL=(url)=>{

    }
    

  return (
    <div>
        <button onClick={()=>{downloadFileAtURL}}></button>
    </div>
  )
}

export default download